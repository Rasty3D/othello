#include <iostream>

void lut_up(int i, int j, unsigned char *mask, unsigned char *color)
{
	memset(mask, 0, 64);
	memset(color, 0, 64);

	if (i > 1)
	{
		//for (int k =
	}
}

void maskNcolor2LUT(
	unsigned char *mask, unsigned char *color, unsigned char *lut)
{

}

int main(int argc, char *argv[])
{
	unsigned char mask[64];
	unsigned char color[64];
	unsigned char lut[16];

	for (int i = 0; i < 8; i++)
	{
		for (int j = 0; j < 8; j++)
		{



		}
	}

	return 0;
}
